import { LogoContainer, LogoText, LogoIcon } from './Logo.styled';

const Logo = () => {
  return (
    <LogoContainer>
      <LogoText>My</LogoText>
      <LogoIcon />
      <LogoText>Post</LogoText>
    </LogoContainer>
  );
};

export default Logo;
